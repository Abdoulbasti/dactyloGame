package cpoo.jeu_dactylo;
import org.fxmisc.richtext.StyleClassedTextArea;
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;

import java.net.URL;

/*public class DactyloGame {
    public void start(Stage primaryStage){

        try
        {
            VBox root = new VBox();

            StyleClassedTextArea textArea = new StyleClassedTextArea();
            textArea.replaceText("Lorem Ipsum");
            //textArea.setId("champsText");

            //textArea.setStyleClass( posDebut, posFin, nomClass);
            //textArea.setStyleClass( 6, 9, "red");
            //textArea.setStyleClass();

            root.getChildren().add(textArea);


            Scene scene = new Scene(root, 320, 150);

            URL url = getClass().getResource("/application.css");
            String path = url.toExternalForm();
            scene.getStylesheets().add(path);
            primaryStage.setTitle("Rich Text Demo");
            primaryStage.setScene(scene);
            primaryStage.show();
        }
        catch (Exception e)
        {
            e.printStackTrace();
        }
    }

    public static void main(String[] args) {
        Application.launch(args);
    }
}*/

public class DactyloGame
{
    public static void main(String[] args) {
        System.out.println("test\ntest\ntest\ntest\ntest\ntest\ntest\ntest\n");
    }
}